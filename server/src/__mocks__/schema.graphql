const fs = require('fs');
const path = require('path');
const { parse } = require('graphql');

// Fall back to reading the file as a string
const filename = path.resolve(__dirname, '../schema.graphql');
const schemaString = fs.readFileSync(filename, 'utf8');

module.exports = parse(schemaString);
